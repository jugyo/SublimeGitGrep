# GitGrep

Sublime Text Plugin to do git grep.

Inspired by [https://bitbucket.org/nicck/sublime-git-grep](https://bitbucket.org/nicck/sublime-git-grep)

## Command

```json
[
  {
    "caption": "Git: Grep",
    "command": "git_grep"
  }
]
```
